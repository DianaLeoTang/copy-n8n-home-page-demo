# n8n 主页复制项目

一个使用 Next.js 和 TypeScript 构建的 n8n 主页复刻项目，展示了现代化的 UI 设计和响应式布局。

## 📋 项目简介

本项目是对 n8n 主页的复刻，展示了 AI 代理工作流模板库的展示页面。项目使用 Next.js 16 构建，采用现代化的 React 组件和 Tailwind CSS 进行样式设计。

## 🛠️ 技术栈

### 核心框架
- **Next.js 16.0.0** - React 全栈框架
- **React 19.2.0** - UI 库
- **TypeScript 5** - 类型安全

### UI 组件库
- **Radix UI** - 无样式、可访问的 UI 组件库
  - Accordion, Alert Dialog, Avatar, Checkbox, Dialog, Dropdown Menu 等
- **shadcn/ui** - 基于 Radix UI 的组件系统
- **Lucide React** - 图标库

### 样式
- **Tailwind CSS 4.1.9** - 实用优先的 CSS 框架
- **tailwindcss-animate** - 动画工具
- **next-themes** - 主题切换支持

### 其他工具
- **react-hook-form** + **zod** - 表单处理和验证
- **Vercel Analytics** - 分析工具
- **date-fns** - 日期处理
- **recharts** - 图表库
- **sonner** - Toast 通知

## ✨ 功能特性

- 🎨 现代化的深色主题 UI 设计
- 📱 完全响应式布局，支持移动端和桌面端
- 🧩 丰富的 UI 组件库（基于 Radix UI）
- ⚡ 使用 Next.js App Router 实现快速导航
- 🎯 TypeScript 类型安全
- 🌙 主题切换支持（通过 next-themes）

## 🚀 快速开始

### 环境要求

- Node.js 18+ 
- pnpm / yarn / npm

### 安装依赖

使用 pnpm（推荐）:
```bash
pnpm install
```

或使用 yarn:
```bash
yarn install
```

或使用 npm:
```bash
npm install
```

### 开发模式

运行开发服务器：
```bash
pnpm dev
# 或
yarn dev
# 或
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000) 查看项目。

### 构建生产版本

```bash
pnpm build
pnpm start
```

### 代码检查

```bash
pnpm lint
```

## 📁 项目结构

```
copy-n8n-home-page/
├── app/                    # Next.js App Router 目录
│   ├── globals.css        # 全局样式
│   ├── layout.tsx         # 根布局组件
│   └── page.tsx           # 主页组件
├── components/            # React 组件
│   ├── ui/               # UI 组件库（shadcn/ui）
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── avatar.tsx
│   │   └── ...           # 更多 UI 组件
│   └── theme-provider.tsx # 主题提供者
├── hooks/                # 自定义 React Hooks
│   ├── use-mobile.ts
│   └── use-toast.ts
├── lib/                  # 工具函数
│   └── utils.ts          # 通用工具函数
├── public/               # 静态资源
│   ├── n8n-logo.jpg
│   └── ...
├── styles/               # 样式文件
│   └── globals.css
├── next.config.mjs       # Next.js 配置
├── tsconfig.json         # TypeScript 配置
├── tailwind.config.js    # Tailwind CSS 配置
└── package.json          # 项目依赖
```

## 🎨 主要页面

### 主页 (`app/page.tsx`)

展示以下内容：
- 导航栏（包含菜单项和 GitHub 链接）
- Hero 区域（介绍文本和 CTA 按钮）
- AI 代理工作流模板网格（展示 6 个模板卡片）

每个模板卡片包含：
- 多个图标展示
- 模板标题
- 作者信息和认证标识

## 🔧 开发说明

### 添加新组件

组件位于 `components/ui/` 目录，使用 shadcn/ui 风格。新组件可以通过以下方式添加：

1. 使用 shadcn CLI（如果配置了）
2. 手动创建组件文件，遵循现有组件模式

### 样式定制

- 全局样式：`app/globals.css`
- Tailwind 配置：通过 `tailwind.config.js` 或 CSS 变量
- 主题色：使用深色主题 `bg-[#1a1b2e]` 和 `bg-[#252640]`

### TypeScript 配置

项目使用严格的 TypeScript 配置，路径别名 `@/*` 指向项目根目录。

## 📝 注意事项

- 项目配置为忽略 TypeScript 构建错误（`next.config.mjs`）
- 图片未优化（`images.unoptimized: true`）
- 使用 pnpm 作为包管理器（推荐）

## 📄 许可证

本项目仅供学习和参考使用。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**注意**: 这是一个复刻项目，用于展示和学习目的。